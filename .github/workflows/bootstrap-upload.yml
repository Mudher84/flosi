name: Bootstrap Flosi Source

on:
  push:
    branches: [ main ]
    paths:
      - flosi-source.zip

permissions:
  contents: write

jobs:
  unpack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Unpack source
        run: |
          unzip -o flosi-source.zip -d .bootstrap
          cp -a .bootstrap/flosi-native-v1.4-premium-rtl-home/. .
          rm -rf .bootstrap flosi-source.zip .github/workflows/bootstrap-upload.yml
      - name: Commit source tree
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Import Flosi v1.4 premium RTL source"
          git push
